# Project JARJAR

Godot 4.7.2-stable Standard / GDScript + C++ GDExtension / Compatibility renderer / Windows x86_64。

## 作業の進め方

依頼された変更と必要な検証まで完了し、変更内容・検証結果・未確認事項を簡潔に報告する。通常のローカル修正・検証と、変更に起因する失敗の修正・再検証は逐次承認を求めず進める。サブエージェントは原則使わない。

## 必要時の参照先

- 戦闘拡張のビルド・配布は [戦闘拡張のREADME](native/combat/README.md) を参照。ゲーム・テスト・botは戦闘用DLLが必須で、初回と `native/combat/` のC++変更後は `./native/combat/build.ps1` を実行する。
- botの開発・実行は [botのREADME](dev/bot/README.md) を参照。初回と `dev/bot/native/` のC++変更後は `./dev/bot/native/build.ps1` を実行する。`dev/bot/` は配布しない開発用ツール。
- バランス変更の着手前は [バランス調整の意図と補足](docs/balance-intent.md) を読む。この文書は人間が編集し、修正案は会話で提示する。

## 検証

変更の影響に応じて範囲を選ぶ。合格後は、新しい変更・失敗・未解決事項がなければ検証を追加・反復しない。

| 変更内容 | 検証範囲 |
| --- | --- |
| 文書・コメントのみ | 差分と参照先 |
| 影響が限定された挙動変更 | 関連テストを選択して実行 |
| 広範な変更、読み込み・実行基盤の変更 | 影響する側の全回帰 |
| ゲームとbotが共有する処理の変更 | 両方の全回帰 |

リポジトリのルートから、対象に応じて実行する。

```powershell
# ゲーム本体
godot --headless --path . --script res://tests/test_runner.gd
# bot
godot --headless --path . --script res://dev/bot/test_runner.gd
```

限定検証では末尾に `-- --test=<名前>` を加え、関数名の `test_` を除いた名前を完全一致で指定する。詳細表示は同じ `--` の後に `--verbose` を指定する。ビルド・検証の実行ごとに `$LASTEXITCODE` を確認し、0を成功、非0を失敗として扱う。

- テスト追加は主要操作・重要なゲームルール・具体的な不具合の再発防止に絞る。保証を重複させず、実装・現在の調整値・表示文言を写す検査は増やさない。
- 実ユーザー設定を保護するため、テスト設定はランナーで実行ごとにOSの一時ディレクトリへ隔離し、終了時に削除する。結果は標準出力へ表示し、ログ・CSV・検証一覧の保存や出力リダイレクトは行わない。
- 見た目・音・難易度・爽快感は試遊で判断し、DPS比率などを合否条件にしない。性能測定は参考資料とし、通常検証・export・起動・試遊の必須条件にしない。

## プロジェクト固有の境界

- 実装仕様はコードとテストを正とする。進捗・変更履歴・検証記録の別文書は維持しない。
- fetch・pull・push・rebase・amend・tag作成はユーザーの明示指示がある場合だけ行う。
- `build/`、`.godot/`、`.codex/`、`work/` はコミットしない。リポジトリ内に `tools/` やExport Templatesを再作成しない。

バランスに関わる変更では次を守る。

- 検討案・仮説を承認と扱わない。【確定】事項に抵触する依頼は実装せず、矛盾を報告する。承認済みの値・確率・進化時刻・受入閾値の変更にはユーザーの明示承認が必要。
- 調整値の正本は `data/balance/` の `.tres`、唯一の読み込み入口は `data/balance/survival_content_manifest.tres`。コードに本番値や代替の既定値を持たせない。
- 数値調整は指定されたデータ・項目に限定する。計算式・公開項目の追加・データ形式の変更は別の構造変更として扱う。
- 定義Resourceは実行状態の保存先にしない。変更するテスト入力は外部参照も含め `duplicate_deep(Resource.DEEP_DUPLICATE_ALL)` で複製し、共有の定義は読み取り専用とする。
- 公開項目の意味・単位・有効範囲・成立条件はドキュメントコメントとInspectorヒントに示す。時間は整数tick、1秒＝60tick。
- 仕様策定中は数値調整しない。本人の試遊へ渡す直前に一度だけ調整し、成功を確認できなくても反復調整せず、結果と得られた本人の感想を報告して次の判断を待つ。
- 完了報告には変更項目・変更前後の実効値・理由・実行した検証・未確認事項を記載する。
